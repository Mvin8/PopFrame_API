version: '3.10'


services:
  web:
    build: .
    ports:
      - 8000:8000
    # depends_on:
    #   - mongo
    env_file:
      - dev.env
    environment:
      - APP_NAME=popframe_api
      - APP_VERSION=dev
    volumes:
      - ./app:/app/app
      - ./requirements.txt:/app/requirements.txt
    command: /start-reload.sh